service: () -> {
    create: () -> (text);
    get: () -> (variant {Ok:record {btc:record {balance:nat64; address:text}}; Err:record {WalletNotFound:principal}}) query;
    transfer: (principal, principal) -> ();
}
