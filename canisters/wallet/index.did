service: () -> {
    create: () -> (text);
    get: () -> (variant {Ok:record {owner:principal; btcAddress:text}; Err:record {WalletNotFound:principal}}) query;
    transfer: (principal, principal) -> ();
}
